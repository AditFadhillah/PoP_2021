module Color

let trunc v =
 let mutable p = v
 if v < 0 then
  p <- 0
 elif v > 255 then
  p <- 255
 p

let c : int

let add (c_1 c_2): int (r_1,g_1,b_1) : int (r_2,g_2,b_2) : int : int*int*int =

let add c : (r,g,b) : int*int*int =
let add 
 let r <- trunc r
 let g <- trunc g
 let b <- trunc b
 (r,g,b)
 
let scale a c =
 let r <- trunc(ar)
 let g <- trunc(ag)
 let b <- trunc(ab)
 (r,g,b)
 

let gray c =
 (trunc(r) + trunc(g) + trunc(b))/3

